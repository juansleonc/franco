services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: franco
      POSTGRES_PASSWORD: franco
      POSTGRES_DB: franco_development
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
  api:
    build:
      context: ./api
    environment:
      DATABASE_URL: postgres://franco:franco@postgres:5432/franco_development
      REDIS_URL: redis://redis:6379/0
      RAILS_ENV: development
    depends_on:
      - postgres
      - redis
    ports:
      - "3000:3000"
    volumes:
      - ./api:/app
volumes:
  postgres_data:
  redis_data:
